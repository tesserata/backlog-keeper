version: '3'
services:
  bot:
    build:
      context: .
      dockerfile: ./Dockerfile
    env_file:
      - .env
    restart: unless-stopped
    container_name: backlog_keeper
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
